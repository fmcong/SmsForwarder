# 2026-07-18

## 代码检查 + 修复 + GitHub Workflow 排错

### Workflow 失败原因与修复
- 根因1：APK 输出路径写反。模块为 `:app`，正确路径是 `app/build/outputs/apk/release/`，但 `Weekly_Build.yml` 与 `Release.yml` 都写成 `build/app/outputs/apk/release`，导致 `output-metadata.json` 与 APK 找不到而失败。已修正。
- 根因2：`Release.yml` 含非法配置 `pull_request: tags:`（pull_request 不支持 tags），会导致整个 workflow 校验失败无法运行。已移除。
- 根因3：`Release.yml` 用已归档的 `actions/create-release@v1` + 已损坏的 `alexellis/upload-assets@0.2.2`，且缺 `permissions: contents: write`（创建 release 会 403）。已替换为 `softprops/action-gh-release@v2` 并加权限。
- 其它：Release 原先未把 keystore 复制到 `app/`（与 Weekly_Build 不一致），已补复制步骤；Weekly_Build 清理旧运行步骤加 `continue-on-error`。
- 注意：`actions/checkout@v5`/`upload-artifact@v5` 均已发布（非失败原因），保留。

### 代码修复
- `SM4Crypt.doSM4`：异常时不再 `printStackTrace`+返回空数组（调用方误判成功），改为直接抛异常；`ActionWorker` 已 try/catch 包裹，安全。
- 凭据脱敏：`WeworkAgentUtils`(corpsecret+代理密码)、`WebhookUtils`/`TelegramUtils`/`DingtalkInnerRobotUtils`(代理密码) 日志改为 `******`。
- `App.kt` `SimInfoList` 改用 `ConcurrentHashMap`（广播/WorkManager 并发读写安全）。
- `EmailUtils` 三处 keystore `FileInputStream` 用 `use{}` 关闭（含 283 行重复新建 FileInputStream 的泄漏）。

### 未改动（已知限制，需权衡）
- 全局 `Authenticator.setDefault`（SOCKS 代理鉴权）：OkHttp 无 per-client SOCKS 鉴权 API，改动会破坏 SOCKS，保留。
- `EmailUtils` 内 `runBlocking` 嵌套阻塞：重构为 suspend 风险较大，未改。
- 大量 `printStackTrace`：仅修复了导致静默失败的 SM4 处，其余保留。

## 第二轮：UI/数据层代码修复（用户要求"全修"）
- `RuleRecyclerAdapter`/`TaskSettingAdapter` `onItemMove`：clamp toPosition 到合法范围，修复拖到列表外 IndexOutOfBounds 崩溃。
- `AppDatabase` 迁移 `MIGRATION_18_19`：SQL 中标签单引号转义（`''`），避免迁移失败打不开库。
- `ConvertersSenderList.stringToObject`：空串/非数字保护，避免 NumberFormatException 导致整条 Rule 查询失败。
- `ForegroundService`：新增 `serviceScope` 并在 `onDestroy` 移除 `observeForever` 观察者(`frpcObserver`/`alarmObserver`)+`compositeDisposable.clear()`+`serviceScope.cancel()`；3 处 `GlobalScope.async` 改为 `serviceScope.launch`。
- `SettingsFragment`：新增 `onDestroyView` 移除 `appListObserver`(`observeStickyForever`)；主线程 `Thread.sleep(200)` 改 `v.postDelayed`。
- `RulesEditFragment`：新增 `onDestroyView` 移除观察者；`testRule` 裸 `Thread` 改 `lifecycleScope.launch(Dispatchers.IO)`。
- `LogsFragment`：刷新时 `collectJob?.cancel()` 后再启动新收集，避免每次刷新叠加协程。
- `MsgPagingAdapter`/`TaskPagingAdapter`：`areContentsTheSame` 由 `===` 改为 `==`（实体是 data class，自动内容相等）。
- `MsgViewModel`：`getOtherCondition` 改为参数化查询(`?`+`selectionArgs`)，杜绝 LIKE/时间 SQL 注入；主查询与 `deleteAll` 同步参数化。
- `NotificationService`：新增 `serviceScope`+`onDestroy` 取消；`onNotificationPosted` 中 `Core.rule.getRuleList` 主线程查库改 `serviceScope.launch(Dispatchers.IO)`。
- `TaskUtils`/`HttpServerUtils` 共享状态均为 `by SharedPreference`（每次读取独立快照），并发崩溃风险低，未改动以免破坏持久化。
- 注：`AppDatabase.allowMainThreadQueries()` 未移除（移除需把大量 DAO 改为 suspend 并改所有调用方，属大型重构，留待单独处理）。

### 提交情况
- 第一轮修复已提交为 `0adc202f`（9 文件），用户用 GitHub Desktop 自行推送（本环境 TLS 无法连 GitHub）。
- 第二轮修复尚未提交（工作树有改动，待用户确认后提交/推送）。
